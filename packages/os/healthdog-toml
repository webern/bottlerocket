metrics_url = "{{settings.healthdog.metrics-url}}"
send_metrics = "{{settings.healthdog.send-metrics}}"
service_health = [{{#each settings.healthdog.service-health as |service| ~}}"{{service}}",{{/each~}}]
seed = {{settings.updates.seed}}
version_lock = "{{settings.updates.version-lock}}"
ignore_waves = {{settings.updates.ignore-waves}}
